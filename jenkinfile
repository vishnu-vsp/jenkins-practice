pipeline
{
    agent any
    stages
    {
        stage('build')
        {
            steps
            {
              echo "Building the project..."
              sh 'pip install -r requirements.txt'
            }
        }
        stage('test')
        {
            steps
            {
              echo "Running tests..."
              sh 'pytest'
            }
        }
        stage('deploy')
        {
            steps
            {
              echo "Deploying the project..."
            }
        }
    }
}
